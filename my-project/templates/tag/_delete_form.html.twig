
<form method="post" action="{{ path('tag_delete', {'id': tag.id}) }}" onsubmit="return confirm('Etes vous sur de vouloir supprimer ?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tag.id) }}">
    <button class="btn btnhome mt-4">Supprimer</button>
</form>
